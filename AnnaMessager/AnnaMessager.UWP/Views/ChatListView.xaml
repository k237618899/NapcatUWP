<mvvm:MvxWindowsPage 
    x:Class="AnnaMessager.UWP.Views.ChatListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mvvm="using:MvvmCross.Uwp.Views"
    xmlns:converters="using:AnnaMessager.UWP.Converters"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <mvvm:MvxWindowsPage.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- 聊天項目樣式 -->
        <Style x:Key="ChatItemStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Grid x:Name="ContentBorder" 
                              Background="{TemplateBinding Background}"
                              BorderBrush="{TemplateBinding BorderBrush}"
                              BorderThickness="{TemplateBinding BorderThickness}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <VisualState.Setters>
                                            <Setter Target="ContentBorder.Background" 
                                                   Value="{ThemeResource SystemControlBackgroundListLowBrush}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <VisualState.Setters>
                                            <Setter Target="ContentBorder.Background" 
                                                   Value="{ThemeResource SystemControlBackgroundListMediumBrush}"/>
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <ContentPresenter x:Name="ContentPresenter"
                                            ContentTransitions="{TemplateBinding ContentTransitions}"
                                            Content="{TemplateBinding Content}"
                                            ContentTemplate="{TemplateBinding ContentTemplate}"
                                            Margin="{TemplateBinding Padding}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </mvvm:MvxWindowsPage.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 標題欄 -->
        <Grid Grid.Row="0" Padding="24,16">
            <TextBlock Text="聊天" 
                       Style="{StaticResource TitleTextStyle}"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center"/>
        </Grid>

        <!-- 搜索和過濾欄 -->
        <Grid Grid.Row="1" Margin="16,0,16,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 搜索框 -->
            <TextBox Grid.Row="0"
                     x:Name="SearchBox" 
                     Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     PlaceholderText="搜尋聊天、聯絡人..."
                     Margin="0,0,0,12"
                     CornerRadius="8"
                     Background="{ThemeResource SystemControlBackgroundAltHighBrush}"
                     BorderBrush="Transparent"
                     TextChanged="SearchBox_TextChanged">
                <TextBox.Resources>
                    <Style TargetType="TextBox">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TextBox">
                                    <Grid>
                                        <Border x:Name="BorderElement"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="{TemplateBinding CornerRadius}"/>
                                        <Grid Margin="12,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <FontIcon Grid.Column="0" 
                                                     Glyph="&#xE721;" 
                                                     FontSize="16"
                                                     Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                                     Margin="0,0,8,0"/>
                                            <ScrollViewer Grid.Column="1"
                                                         x:Name="ContentElement"
                                                         HorizontalScrollMode="Hidden"
                                                         HorizontalScrollBarVisibility="Hidden"
                                                         VerticalScrollMode="Hidden"
                                                         VerticalScrollBarVisibility="Hidden"/>
                                        </Grid>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TextBox.Resources>
            </TextBox>

            <!-- 過濾按鈕 -->
            <StackPanel Grid.Row="1" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Left">
                <ToggleButton x:Name="UnreadFilterButton"
                              Content="未讀"
                              IsChecked="{Binding ShowOnlyUnread, Mode=TwoWay}"
                              Style="{StaticResource FilterToggleButtonStyle}"
                              Margin="0,0,8,0"
                              Command="{Binding ToggleUnreadFilterCommand}"/>

                <ToggleButton x:Name="PinnedFilterButton"
                              Content="置頂"
                              IsChecked="{Binding ShowOnlyPinned, Mode=TwoWay}"
                              Style="{StaticResource FilterToggleButtonStyle}"
                              Command="{Binding TogglePinnedFilterCommand}"/>
            </StackPanel>
        </Grid>

        <!-- 聊天列表 -->
        <ListView Grid.Row="2" 
                  x:Name="ChatListViewControl"
                  ItemsSource="{Binding FilteredChatList}"
                  SelectionMode="Single"
                  SelectionChanged="ChatListView_SelectionChanged"
                  ItemContainerStyle="{StaticResource ChatItemStyle}"
                  ShowsScrollingPlaceholders="False">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Height="72" 
                          Background="Transparent"
                          RightTapped="ChatItem_RightTapped">
                        <Grid.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="置頂聊天" 
                                               Icon="Pin"
                                               Command="{Binding DataContext.PinChatCommand, ElementName=ChatListViewControl}"
                                               CommandParameter="{Binding}"
                                               Visibility="{Binding IsPinned, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=inverted}"/>
                                <MenuFlyoutItem Text="取消置頂" 
                                               Icon="UnPin"
                                               Command="{Binding DataContext.PinChatCommand, ElementName=ChatListViewControl}"
                                               CommandParameter="{Binding}"
                                               Visibility="{Binding IsPinned, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <MenuFlyoutSeparator/>
                                <MenuFlyoutItem Text="靜音聊天" 
                                               Icon="Mute"
                                               Command="{Binding DataContext.MuteChatCommand, ElementName=ChatListViewControl}"
                                               CommandParameter="{Binding}"
                                               Visibility="{Binding IsMuted, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=inverted}"/>
                                <MenuFlyoutItem Text="取消靜音" 
                                               Icon="Volume"
                                               Command="{Binding DataContext.MuteChatCommand, ElementName=ChatListViewControl}"
                                               CommandParameter="{Binding}"
                                               Visibility="{Binding IsMuted, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <MenuFlyoutSeparator/>
                                <MenuFlyoutItem Text="刪除聊天" 
                                               Icon="Delete"
                                               Command="{Binding DataContext.DeleteChatCommand, ElementName=ChatListViewControl}"
                                               CommandParameter="{Binding}"/>
                            </MenuFlyout>
                        </Grid.ContextFlyout>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- 頭像區域 -->
                        <Grid Grid.Column="0" Width="48" Height="48">
                            <!-- 頭像背景 -->
                            <Ellipse Fill="{Binding AvatarColor, FallbackValue='#FF0078D4'}" 
                                    Visibility="{Binding HasAvatar, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=inverted}"/>

                            <!-- 頭像圖片 -->
                            <Ellipse Visibility="{Binding HasAvatar, Converter={StaticResource BoolToVisibilityConverter}}">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding AvatarImage}" Stretch="UniformToFill"/>
                                </Ellipse.Fill>
                            </Ellipse>

                            <!-- 載入指示器 -->
                            <ProgressRing Width="20" Height="20"
                                         IsActive="{Binding IsLoadingAvatar}"
                                         Visibility="{Binding IsLoadingAvatar, Converter={StaticResource BoolToVisibilityConverter}}"/>

                            <!-- 頭像文字 (當沒有圖片時顯示) -->
                            <TextBlock Text="{Binding Name, Converter={StaticResource FirstLetterConverter}}"
                                      FontSize="20"
                                      FontWeight="SemiBold"
                                      Foreground="White"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Visibility="{Binding HasAvatar, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=inverted}"/>

                            <!-- 在線狀態指示器 (僅好友聊天) -->
                            <Ellipse Width="14" Height="14"
                                    Fill="{StaticResource OnlineBrush}"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Bottom"
                                    Margin="0,0,-2,-2"
                                    Visibility="{Binding IsGroup, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=inverted}"/>

                            <!-- 置頂圖標 -->
                            <FontIcon Glyph="&#xE718;"
                                     FontSize="12"
                                     Foreground="{StaticResource AccentBrush}"
                                     HorizontalAlignment="Left"
                                     VerticalAlignment="Top"
                                     Margin="-4,-4,0,0"
                                     Visibility="{Binding IsPinned, Converter={StaticResource BoolToVisibilityConverter}}"/>

                            <!-- 靜音圖標 -->
                            <FontIcon Glyph="&#xE74F;"
                                     FontSize="12"
                                     Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                     HorizontalAlignment="Right"
                                     VerticalAlignment="Top"
                                     Margin="0,-4,-4,0"
                                     Visibility="{Binding IsMuted, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        </Grid>

                        <!-- 聊天信息區域 -->
                        <StackPanel Grid.Column="1" 
                                   Margin="12,0,8,0"
                                   VerticalAlignment="Center">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- 聊天名稱 -->
                                <TextBlock Grid.Column="0"
                                          Text="{Binding Name}" 
                                          Style="{StaticResource ChatNameTextStyle}"
                                          TextTrimming="CharacterEllipsis"
                                          MaxLines="1"/>

                                <!-- 群組成員數量 -->
                                <TextBlock Grid.Column="1"
                                          Text="{Binding MemberCount}"
                                          FontSize="12"
                                          Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                          Margin="4,0,0,0"
                                          Visibility="{Binding IsGroup, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </Grid>

                            <!-- 最後消息 -->
                            <TextBlock Text="{Binding DisplayLastMessage, FallbackValue={Binding LastMessage}}"
                                      Style="{StaticResource LastMessageTextStyle}"
                                      TextTrimming="CharacterEllipsis"
                                      MaxLines="1"
                                      Margin="0,2,0,0"/>
                        </StackPanel>

                        <!-- 時間和未讀數區域 -->
                        <StackPanel Grid.Column="2" 
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Right"
                                   Margin="8,0,0,0">
                            <!-- 時間 -->
                            <TextBlock Text="{Binding DisplayTime}"
                                      Style="{StaticResource TimeTextStyle}"
                                      HorizontalAlignment="Right"/>

                            <!-- 未讀數量徽章 -->
                            <Border Background="{StaticResource UnreadBadgeBrush}"
                                   CornerRadius="10"
                                   MinWidth="20"
                                   Height="20"
                                   Margin="0,4,0,0"
                                   Visibility="{Binding HasUnreadMessages, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="{Binding UnreadCountDisplay}"
                                          FontSize="11"
                                          FontWeight="Bold"
                                          Foreground="White"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Padding="6,0"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>

            <!-- 空狀態 -->
            <ListView.Template>
                <ControlTemplate TargetType="ListView">
                    <Grid>
                        <ScrollViewer x:Name="ScrollViewer"
                                     HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
                                     HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                     VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
                                     VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                                     ZoomMode="Disabled">
                            <ItemsPresenter/>
                        </ScrollViewer>

                        <!-- 空狀態顯示 -->
                        <StackPanel HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Visibility="{Binding FilteredChatList.Count, Converter={StaticResource EmptyListToVisibilityConverter}}">
                            <FontIcon Glyph="&#xE8BD;"
                                     FontSize="48"
                                     Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                     HorizontalAlignment="Center"
                                     Margin="0,0,0,16"/>
                            <TextBlock Text="暫無聊天"
                                      FontSize="18"
                                      Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                      HorizontalAlignment="Center"/>
                            <TextBlock Text="點擊聯絡人開始聊天"
                                      FontSize="14"
                                      Foreground="{ThemeResource SystemControlForegroundBaseLowBrush}"
                                      HorizontalAlignment="Center"
                                      Margin="0,8,0,0"/>
                        </StackPanel>
                    </Grid>
                </ControlTemplate>
            </ListView.Template>
        </ListView>

        <!-- 載入指示器 -->
        <StackPanel Grid.Row="2" 
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="{Binding IsRefreshing, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressRing Width="40" Height="40" IsActive="True"/>
            <TextBlock Text="載入中..."
                      FontSize="14"
                      Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                      HorizontalAlignment="Center"
                      Margin="0,8,0,0"/>
        </StackPanel>

        <!-- 下拉刷新 -->
        <RefreshContainer Grid.Row="2" 
                         RefreshRequested="RefreshContainer_RefreshRequested">
            <!-- RefreshContainer 的內容將由 ListView 填充 -->
        </RefreshContainer>
    </Grid>
</mvvm:MvxWindowsPage>