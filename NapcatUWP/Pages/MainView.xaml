<Page
    x:Class="NapcatUWP.Pages.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NapcatUWP.Pages"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <local:SidebarWidthToVisibilityConverter x:Key="SidebarWidthToVisibilityConverter" />
    </Page.Resources>

    <Grid x:Name="RootGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="SidebarColumn" Width="0" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  側邊欄  -->
        <Grid Grid.Column="0" Background="#FF2D3E50">
            <StackPanel>
                <StackPanel Margin="8,32,0,16" Orientation="Horizontal">
                    <Ellipse
                        Width="40"
                        Height="40"
                        Fill="LightBlue" />
                    <StackPanel Margin="8,0,0,0" Visibility="{Binding ElementName=SidebarColumn, Path=Width.Value, Converter={StaticResource SidebarWidthToVisibilityConverter}}">
                        <TextBlock
                            x:Name="TextUser"
                            FontWeight="Bold"
                            Foreground="White"
                            Text="Jacob Hsu" />
                        <TextBlock
                            x:Name="TextID"
                            FontSize="12"
                            Foreground="White"
                            Text="+1 702 551 6183" />
                    </StackPanel>
                </StackPanel>
                <ListView
                    Margin="0,8,0,0"
                    SelectionChanged="SidebarListView_SelectionChanged"
                    Visibility="{Binding ElementName=SidebarColumn, Path=Width.Value, Converter={StaticResource SidebarWidthToVisibilityConverter}}">
                    <ListViewItem Content="Chats" Foreground="White" />
                    <ListViewItem Content="Contacts" Foreground="White" />
                    <ListViewItem Content="Groups" Foreground="White" />
                    <ListViewItem Content="Settings" Foreground="White" />
                    <ListViewItem Content="Saved Messages" Foreground="White" />
                    <ListViewItem Content="Log Out" Foreground="DarkRed" />
                </ListView>
            </StackPanel>
        </Grid>

        <!--  主內容區域  -->
        <Grid
            x:Name="MainContentGrid"
            Grid.Column="1"
            Background="#FF23272A">
            <Rectangle
                x:Name="OverlayRect"
                Canvas.ZIndex="99"
                Fill="Transparent"
                PointerPressed="OverlayRect_PointerPressed"
                Visibility="Collapsed" />
            <!--  Changed Grid.ZIndex to Canvas.ZIndex  -->
            <StackPanel>
                <!--  頂部搜索欄  -->
                <StackPanel
                    Padding="12,8"
                    Background="#FF23272A"
                    Orientation="Horizontal">
                    <Button
                        Width="40"
                        Height="40"
                        Background="Transparent"
                        Click="HamburgerButton_Click">
                        <FontIcon
                            FontFamily="Segoe MDL2 Assets"
                            FontSize="20"
                            Foreground="White"
                            Glyph="&#xE700;" />
                    </Button>
                    <TextBox
                        Width="300"
                        Height="40"
                        Margin="12,0,0,0"
                        Padding="0,4,0,0"
                        VerticalContentAlignment="Center"
                        PlaceholderText="Search (Chats)" />
                </StackPanel>
                <!--  聊天列表  -->
                <ListView Margin="0,8,0,0">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Height="64" Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="64" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Ellipse
                                    Width="48"
                                    Height="48"
                                    Margin="8"
                                    VerticalAlignment="Center"
                                    Fill="Gray" />
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock
                                        FontWeight="Bold"
                                        Foreground="White"
                                        Text="群組名稱" />
                                    <TextBlock
                                        FontSize="12"
                                        Foreground="LightGray"
                                        Text="最後消息摘要..." />
                                </StackPanel>
                                <StackPanel
                                    Grid.Column="2"
                                    Margin="8"
                                    VerticalAlignment="Center">
                                    <TextBlock
                                        FontSize="12"
                                        Foreground="LightGray"
                                        Text="上午 08:27" />
                                    <Border
                                        Margin="0,4,0,0"
                                        Padding="6,2"
                                        Background="#444"
                                        CornerRadius="12">
                                        <TextBlock
                                            FontSize="12"
                                            Foreground="White"
                                            Text="622" />
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>
        <Grid
            x:Name="LogoutMask"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="#80000000"
            Canvas.ZIndex="999"
            Visibility="Collapsed">
            <ProgressRing
                x:Name="LogoutProgressRing"
                Width="80"
                Height="80"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Foreground="#7e6ca8"
                IsActive="True" />
        </Grid>
    </Grid>
</Page>